编译系统分配给整型数据4个字节，整数用补码的形势存放，一个正数的补码就是他的二进制形式，负数的补码方法为：先将此数的绝对值写成二进制形势，然后对其所有二进制位按位取反，再加1。在有符号的整型数据存储中，存储单元最左边一位是用来表示符号的，0位正，1为负。
代码通过指针找到数据存储地址，直接读取整数存储的二进制形式，输出时编译系统会将二进制形式自动转换成十六进制，十六进制和二进制转换方便，并且便于显示，一个十六进制位相当于四个二进制位，一个字节就转换成了两个十六进制位，整数分配了四个字节，所以 就显示了八个十六进制位。
